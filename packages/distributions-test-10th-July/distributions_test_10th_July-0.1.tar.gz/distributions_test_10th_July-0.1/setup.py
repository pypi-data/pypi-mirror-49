from setuptools import setup

setup(name='distributions_test_10th_July',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_test_10th_July'],
      zip_safe=False)
