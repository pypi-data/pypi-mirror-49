from setuptools import setup, find_packages

setup(name='dental',
      version='0.0.1',
      description='The dental python library.',
      author='PeterPyPan',
      packages=find_packages('.'),
      )
