#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['preconvert_numpy']

package_data = \
{'': ['*']}

install_requires = \
['preconvert >=0.0.2', 'numpy']

extras_require = \
{'bson': ['pymongo'], 'msgpack': ['msgpack'], 'simplejson': ['simplejson']}

entry_points = \
{'preconvert.converters': ['numpy = preconvert_numpy:converters']}

setup(name='preconvert_numpy',
      version='0.0.3',
      description='Preconverts common numpy types to their serializable (jsonifiable, msgpackable, etc...) form.',
      author='Timothy Edmund Crosley',
      author_email='timothy.crosley@gmail.com',
      url='https://github.com/timothycrosley/preconvert_numpy',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.5',
     )
