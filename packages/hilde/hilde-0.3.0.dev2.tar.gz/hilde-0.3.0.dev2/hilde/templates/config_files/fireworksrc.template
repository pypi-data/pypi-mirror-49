[remote_setup]
launch_dir = "."
remote_host = ["remote.host.path"]
remote_config_dir = ["/path/to/remote/home/.fireworks/"]
reserve = True

[remote_host_auth]
remote_user = remote_username
remote_password = null
gss_auth = True

[remote_queue_param]
njobs_queue = 0
njobs_block = 500

[launch_params]
nlaunches = 0
sleep_time = 60
tasks2queue = ["hilde.relaxation.bfgs.relax""hilde.fireworks.tasks.calculate_wrapper.wrap_calc_socket", "hilde.k_grid.converge_kgrid.converge_kgrid", "hilde.fireworks.tasks.calculate_wrapper.wrap_calculate" ]
