# {{{ author.name }}}
# {{{ project.name }}}
# coding: convert some string responses to integers

#articles$Source = factor(articles$source.id,
#                         levels=c(1:8),
#                         labels=source_labels)

clean_tmp()
