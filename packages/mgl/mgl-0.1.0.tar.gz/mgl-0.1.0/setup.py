from setuptools import Extension, setup

# ext = Extension(
#     name='mgl',
#     sources=['mgl.cpp'],
#     extra_compile_args=['-fpermissive'],
# )

setup(
    name='mgl',
    version='0.1.0',
    # ext_modules=[ext],
)
