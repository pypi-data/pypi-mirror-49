from setuptools import setup

setup(name='gaus_binom_distributions',
      version='0.1',
      description='Gaussian & Binomial Distributions',
      packages=['gaus_binom_distributions'],
      zip_safe=False)
