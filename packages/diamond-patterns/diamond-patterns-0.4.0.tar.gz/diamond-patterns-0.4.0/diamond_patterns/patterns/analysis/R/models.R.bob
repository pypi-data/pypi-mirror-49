# {{{ author.name }}}
# {{{ project.name }}}
# models: all the linear models, represented as S expressions

models = list()
models$base = dv ~ iv1 + iv2

# fit all models
# fits_glm = list()
# fits_glm$base = glm(models$base, data=sub_articles)

# fit with lm in order to get R^2
# fits_lm = list()
# fits_lm$base = lm(models$base, data=sub_articles)

clean_tmp()
