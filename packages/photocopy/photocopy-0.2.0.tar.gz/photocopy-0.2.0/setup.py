#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['photocopy']

package_data = \
{'': ['*']}

install_requires = \
['docopt >=0.6.2', 'exifread >=2.1.2']

entry_points = \
{'console_scripts': ['photocopy = photocopy:main']}

setup(name='photocopy',
      version='0.2.0',
      description='Copy your photos from an SD card or camera to a specific directory.',
      author='Stavros Korokithakis',
      author_email='hi@stavros.io',
      url='https://github.com/skorokithakis/photocopy/',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
